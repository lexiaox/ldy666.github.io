#memset( 数组名, -1, sizeof( 数组名));
##函数原型

···

void *memset(void *str, int c, size_t n);

```

- str: 要填充的内存块的指针
- c: 要设置的值（以int形式传递，但实际使用每个字节的低8位）
- n: 要设置的字节数

##具体作用

```
int arr[100];
memset(arr, 0, sizeof(arr));
```

这行代码将：

arr 数组的 100 * sizeof(int) 个字节全部设置为0

相当于将数组的所有元素初始化为0